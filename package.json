{
  "name": "news-aggregator",
  "version": "1.0.0",
  "description": "A news aggregator that fetches headlines from 27 trusted sources and ranks them",
  "main": "src/fetch-feeds.js",
  "type": "module",
  "scripts": {
    "start": "node src/fetch-feeds.js",
    "build": "npm run start && node src/dedupe-and-rank.js && node src/build-site.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "news",
    "rss",
    "aggregator"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "node-fetch": "^3.3.2",
    "rss-parser": "^3.13.0",
    "dotenv": "^16.3.1"
  }
}
